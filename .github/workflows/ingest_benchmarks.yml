name: Ingest Benchmark Artifacts`n`non:`n  repository_dispatch:`n    types: [benchmark_upload]`n`njobs:`n  ingest:`n    runs-on: ubuntu-latest`n    steps:`n      - uses: actions/checkout@v3`n      - name: Setup Python`n        uses: actions/setup-python@v4`n        with:`n          python-version: "3.10"`n      - name: Download Artifact`n        run: |`n          echo "Simulating cross-repo artifact download with gh run download"`n          echo "Payload: ${{ github.event.client_payload.url }}"`n      - name: Parse Data`n        run: python scripts/benchmarks/benchmark_parser.py downloaded.csv || echo "Parser not yet merged"`n
