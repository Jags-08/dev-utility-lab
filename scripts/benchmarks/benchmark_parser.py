import csv`nimport json`nimport sys`n`ndef parse_benchmark_csv(filepath):`n    results = []`n    try:`n        with open(filepath, mode="r", encoding="utf-8") as f:`n            reader = csv.DictReader(f)`n            for row in reader:`n                results.append(row)`n        return results`n    except Exception as e:`n        print(f"Error parsing {filepath}: {e}")`n        return None`n`nif __name__ == "__main__":`n    if len(sys.argv) > 1:`n        data = parse_benchmark_csv(sys.argv[1])`n        print(json.dumps(data, indent=2))`n    else:`n        print("Usage: python benchmark_parser.py <path_to_csv>")
